# 추가 계획 로그

> **넣기로 한 것**의 기록. 무엇을, 왜, 언제, 무슨 대가를 치르고 넣는지.
> 제거는 [delete_plan.md](delete_plan.md), 전체 로드맵은 [plan.md](plan.md).

**기록 규칙**
- 새 항목은 `§ 대기열` 맨 아래에 추가
- 착수하면 `§ 진행 중`, 끝나면 `§ 완료`로 **이동**하고 완료일과 실제 결과를 적는다
- 계획대로 안 됐으면 그것도 적는다. 로그의 가치는 정직함에 있다

**ID 접두사**

| 접두사 | 영역 |
|---|---|
| `CORE` | 랭킹 엔진 · 도메인 로직 |
| `UX` | 화면 · 인터랙션 |
| `DATA` | 수집 · 정규화 |
| `INFRA` | 백엔드 · 배포 |
| `PWA` | 웹앱 고유 기능 |
| `SAFETY` | 모더레이션 · 신고 |
| `LEGAL` | 법무 · 정책 |

**항목 형식**

```
### [ID] 제목
- 단계 / 우선순위 / 예상 규모
- 왜: 이걸 왜 넣는가
- 무엇: 구체적으로 무엇이 만들어지는가
- 완료 조건: 무엇을 보고 끝났다고 판단하는가
- 대가: 무엇을 잃는가 (없으면 "없음"이라고 쓴다)
```

---

## 완료

### [UX-10] 뉴스 브리핑 — 방에서 내용을 읽게 한다
- Phase 3 / 최우선 / M
- **완료**: 2026-08-28
- 왜: 방에 들어와도 헤드라인만 보여서 사람들이 **내용을 모른 채 대화하고 있었다**
- 근본 원인 (감사에서 드러남)
  1. 구글 트렌드의 `ht:news_item_snippet` 이 **늘 비어 있다**(확인: 30건 전부 0자).
     `summary` 가 `newsSnippet ?? newsTitle` 로 폴백해 계속 **기사 제목**이 들어갔다.
     요약처럼 보이던 게 실은 제목이었다
  2. 정작 언론사 RSS `<description>` 에 기사 리드가 83~497자씩 오는데
     파서가 `<title>` 만 읽고 버렸다
- 무엇
  - `issues.articles jsonb` — 매칭된 기사의 리드·언론사·링크 보관
  - `summary` 를 가장 충실한 리드로 교체. 없으면 제목 폴백(그때는 `articles` 로 구분 가능)
  - 방 Catch-up 카드에 `뉴스 브리핑` 섹션. 3줄 클램프 + 탭하면 펼침 + `원문 보기`
  - 트렌드 news_item 3건도 담아 **매칭 안 된 이슈에서도 화면이 비지 않게**
- 결과: 진짜 요약 **0건 → 5/10건**, 기사는 10건 전부에 붙음.
  Edge Function 과 로컬 수집기가 같은 5/10 을 낸다
- 대가: 언론사 매칭이 6~7/10 이라 나머지는 제목·링크만 남는다.
  소스 확대로 올릴 수 있지만 수치를 보고 판단한다
- **저작권**: RSS 리드는 언론사가 배포 목적으로 넣은 값이고 표준 RSS 리더가
  그대로 보여주는 값이다. 기사 본문은 가져오지 않는다.
  인용마다 언론사명과 원문 링크를 함께 노출한다 → `LEGAL-01` 확인 항목

### [DATA-01] 실시간 수집 — 서버리스로 가동 중
- Phase 3 / 최우선 / L
- **완료**: 2026-08-28
- 왜: 로컬 Dart 수집기는 누군가 PC 를 켜둬야 했다. 그러면 배포본의 순위가
  그 PC 전원에 묶인다
- 무엇
  - Edge Function `collect` + `pg_cron` 5분 주기 (`pg_net` 으로 호출)
  - 구글 트렌드 RSS 가 **무엇이 이슈인지 정의**하고,
    언론사 6곳 19개 피드가 **실제로 보도되는지 검증**해 확산도를 만든다
  - service_role 키는 Vault 에 넣고 이름으로만 참조 — `cron.job` 은 평문 조회가 된다
  - 전부 공식 RSS 신디케이션 피드. HTML 스크래핑 없음
- 결과: 수동 호출 upsert 10건·교차확인 7건·2.1초.
  cron 자동 실행 후 4초 만에 데이터 갱신 확인
- 대가: 같은 로직이 Deno(배포)와 Dart(로컬 개발) 두 곳에 있다.
  판단 규칙을 바꾸면 양쪽을 함께 고쳐야 한다

### [UX-09] 실시간 체감 — 갱신 시계 · 순위 애니메이션
- Phase 1 / 높음 / M
- **완료**: 2026-08-28
- 왜: 데이터는 5분마다 실제로 바뀌는데 화면에서 그게 하나도 안 보였다.
  4분 59초 동안 살아있다는 근거가 없었다
- 무엇
  - 갱신 시계 — `12초 전 갱신 · 다음 4:48` 이 매초 움직인다.
    데이터가 안 바뀌어도 정직하게 변하는 유일한 값
  - 라이브 도트를 데이터 상태와 연결. 예전엔 수집이 멈춰도 똑같이 깜빡여 **거짓 신호**였다.
    이제 갱신 순간에만 링이 퍼지고, 11분 넘게 끊기면 회색으로 죽는다
  - 변경된 카드만 하이라이트 (오르면 붉게, 내리면 푸르게)
  - 순위 이동 애니메이션 — `Stack` + `AnimatedPositioned`, 패키지 추가 없음
  - `글 0 · 추천 0 · 0명 접속` 제거. 사용자가 없는 동안 모든 카드가 0을 달고 있으면
    화면이 죽었다고 광고하는 셈이다. 대신 늘 존재하는 **검색량**을 앞에 세웠다
- 대가: 카드 높이를 118px 로 고정해야 위치 계산이 성립한다.
  글자 배율 2.0 검증은 아직 (→ A11Y-01)

### [AUTH-01] 계정 없는 익명 인증
- Phase 2 / 최우선 / M
- **완료**: 2026-08-27
- 왜: 이슈 서비스의 생명은 진입 속도다. 카톡 링크를 받고 3초 안에 대화를 봐야 하는데
  로그인 벽을 세우면 전환율이 무너진다 ([idea.md](idea.md) IDEA-X1)
- 무엇
  - Supabase 익명 로그인. 앱 시작 시 자동 실행되고 로그인 화면 자체가 없다
  - 프로필 테이블 없음. 이메일·전화번호·표시 이름을 저장하지 않는다
  - 닉네임은 `(uid + 방id)` FNV 해시로 생성 →
    [anon_identity.dart](lib/core/identity/anon_identity.dart)
  - `post_likes`/`post_reactions` 는 RLS로 **본인 행만 조회 가능**.
    집계 숫자만 공개하고 누가 눌렀는지는 아무도 못 본다
  - config.toml 에서 이메일/SMS 가입을 명시적으로 껐다
- 결과: uid는 ① 자기 글 삭제 ② 1인 1추천 ③ 도배 차단 세 가지에만 쓰인다
- 대가: 브라우저 데이터를 지우면 "내 글"과의 연결이 끊긴다.
  기기 간 동기화도 불가능하다. 익명성의 대가로 받아들인다

### [INFRA-02] Supabase 백엔드 연결
- Phase 2 / 최우선 / L
- **완료**: 2026-08-27
- 왜: Firebase 대신 Supabase를 택한 이유는 **RLS**다. 익명 서비스에서 "누가 무엇을
  할 수 있는가"를 Postgres 정책으로 선언해 두면, 클라이언트 코드가 뚫려도
  DB가 막는다. Firestore 규칙보다 표현력이 높고 SQL로 검증할 수 있다
- 무엇
  - 테이블 7개 + 스토리지 버킷 ([migrations/](supabase/migrations/))
  - `issues.ranks` 를 jsonb 로 비정규화 → Realtime `.stream()` 이 조인을 지원하지
    않는 제약을 우회. 카드에 필요한 모든 값이 한 번에 온다
  - 카운터는 전부 트리거. 클라이언트에 UPDATE 정책을 아예 안 줬다
  - 추천/반응은 `toggle_post_like` / `toggle_post_reaction` RPC.
    서버가 원자적으로 뒤집으므로 경쟁 상태와 중복 추천이 원천 차단된다
  - [supabase_issue_repository.dart](lib/data/repositories/supabase_issue_repository.dart)
- 대가: Realtime 스트림이 조인을 못 해서 비정규화가 강제된다.
  수집기가 `ranks` jsonb 와 `rank_snapshots` 를 둘 다 써야 한다

### [INFRA-04] 백엔드 없이도 뜨는 목 폴백
- Phase 2 / 높음 / S
- **완료**: 2026-08-27
- 왜: Supabase 프로젝트를 만들기 전에도, Docker가 꺼져 있어도, 네트워크가 없어도
  화면은 보여야 한다. "환경 세팅 끝날 때까지 아무것도 못 봄"이 프로토타입의 최대 적이다
- 무엇: `SUPABASE_URL` 이 비었거나 초기화가 실패하면 `MockIssueRepository` 로 폴백.
  현재 백엔드는 설정 탭과 순위 화면 하단에 표시된다
- 대가: 목과 실서버 두 구현을 나란히 유지해야 한다.
  인터페이스가 커질 때마다 양쪽을 고쳐야 함 → [delete_plan.md](delete_plan.md) DEL-01 재검토됨

### [SAFETY-02] 도배 차단 + 신고 자동 블라인드
- Phase 2 / 높음 / M
- **완료**: 2026-08-27
- 왜: 계정이 없으니 "가입 후 N일" 같은 장치를 쓸 수 없다. 남는 수단은 속도 제한뿐이다
- 무엇
  - `enforce_post_rate_limit`: 10초에 3건 초과 시 거절. 한국어 메시지를 그대로 UI에 노출
  - `on_report_created`: 같은 대상 신고 5건 누적 시 자동 `is_hidden`
  - `reports` 유니크 `(대상, 신고자)` — 한 명이 반복 신고로 블라인드를 못 만든다
- 대가: 5건이라는 임계치에 근거가 없다. 실 트래픽 보고 조정해야 한다.
  신고 자체가 공격 수단이 될 수 있어 운영자 검토(SAFETY-01)가 반드시 따라와야 함

### [INFRA-05] Chrome 부재 시 Edge 자동 선택
- Phase 1 / 낮음 / XS
- **완료**: 2026-08-27
- 왜: 이 머신에 Chrome 이 없어서 `flutter run -d chrome` 이 그냥 실패했다.
  브라우저를 하드코딩하면 팀원 머신마다 다른 이유로 깨진다
- 무엇: `run_local.ps1` 이 `flutter devices --machine` 으로 설치된 브라우저를 찾아
  chrome → edge 순으로 고른다. `-Device` 로 수동 지정도 가능.
  `.vscode/launch.json` 에 Edge/Chrome 구성을 모두 넣었다
- 대가: 없음

### [CI-01] 포맷·린트 게이트 활성화
- Phase 1 / 낮음 / XS
- **완료**: 2026-08-27
- 왜: 스캐폴드를 셸 없이 손으로 써서 포매터를 한 번도 못 돌린 상태였다.
  게이트가 비차단이면 없는 것과 같다
- 무엇: `dart format lib test` 실행(9개 파일 정리) → CI 포맷 단계에서
  `continue-on-error` 제거 → `analysis_options.yaml` 의
  `require_trailing_commas` 활성화
- 결과: `flutter analyze` 이슈 0건, 테스트 14개 통과
- 대가: 없음

### [DX-01] Git · CI · MCP · 로컬 실행 구성
- Phase 1 / 중간 / S
- **완료**: 2026-08-27
- 무엇
  - `.gitignore`(env/*.json 시크릿 제외) · `.gitattributes`(줄바꿈 정규화)
  - GitHub Actions: analyze · test · 웹 빌드 + **로컬 Supabase 스택에 마이그레이션과
    시드를 실제로 적용**해 RLS 오타까지 CI에서 잡는다
  - `.mcp.json` — Supabase MCP 서버. 토큰은 `${SUPABASE_ACCESS_TOKEN}` 환경변수 확장으로
    받아 파일에 시크릿이 남지 않는다. `--read-only` 로 잠금
  - `.vscode/launch.json` 3종 + `run_local.ps1`
- 대가: 없음 (포맷 게이트 비차단 문제는 CI-01 에서 해소됨)

### [CORE-01] HotScore 랭킹 엔진
- Phase 0 / 최우선 / M
- **완료**: 2026-08-27
- 왜: 포털 순위 미러링은 대체 가능한 껍데기라서. 자체 로직이 이 서비스의 존재 이유
- 무엇: 순위·상승세·확산도·참여도 4축 가중합 × 신선도 감쇠.
  [lib/domain/ranking/hot_score.dart](lib/domain/ranking/hot_score.dart)
- 결과: 단위 테스트 8종 통과. 로그 스케일 참여도로 거대 방 독점 차단 확인
- 대가: 가중치 초기값에 근거가 없다. 실 데이터로 재튜닝 필요 (→ CORE-03)

### [CORE-02] 정렬 모드 프리셋 3종
- Phase 0 / 높음 / S
- **완료**: 2026-08-27
- 왜: 알고리즘 불만을 선택권으로 전환. 어떤 모드를 쓰는지가 의도 데이터
- 무엇: `균형 / 속보 / 토론`. 모드 전환 시 이전 순위 비교 리셋
- 대가: 사용자마다 다른 순위를 보므로 "1위가 뭐였다"는 대화가 어긋날 수 있음

### [UX-01] 점수 근거 공개 시트
- Phase 0 / 높음 / S
- **완료**: 2026-08-27
- 왜: 실검을 죽인 건 불투명성이었다는 진단([idea.md](idea.md) IDEA-02)
- 무엇: 카드의 점수를 탭하면 5개 세부 점수를 막대그래프로. 계산식도 함께 노출
- 대가: 어뷰저에게도 알고리즘이 공개됨. 확산도 항목이 이를 상쇄한다고 판단

### [UX-02] Catch-up 카드
- Phase 0 / 최우선 / M
- **완료**: 2026-08-27
- 왜: 원본 아이디어의 "나중에 온 사람들"을 화면으로 구현한 것. 제품의 중심
- 무엇: 방 최상단에 요약 + 소스별 순위 + 반응 상위 3개 글 + 관련 키워드
- 결과: [lib/features/room/widgets/catch_up_card.dart](lib/features/room/widgets/catch_up_card.dart)
- 대가: 요약 품질에 서비스 인상이 좌우됨. LLM 요약(DATA-04) 품질이 관건

### [INFRA-00] Flutter PWA 스캐폴드
- Phase 0 / 최우선 / M
- **완료**: 2026-08-27
- 무엇: 프로젝트 구조, Riverpod/go_router, 매니페스트, 스플래시, 설치 프롬프트 훅,
  Repository 인터페이스 분리(백엔드 교체 비용 최소화)
- 대가: 셸 사용 불가 환경이라 수동 작성. 네이티브 플랫폼 폴더와 PNG 아이콘 누락 (→ PWA-01)

---

## 진행 중

_(없음)_

---

## 대기열

우선순위는 **"이 프로토타입이 무엇을 증명해야 하는가"** 로 정렬했다.
증명해야 할 것은 [idea.md](idea.md) `Q-02` — *사람들은 정말 지난 이슈를 읽는가.*
그 답을 얻으려면 진짜 이슈와 진짜 대화가 필요하고, 나머지는 전부 그 다음이다.

```
P0 수집기 ──▶ 진짜 이슈 ──▶ 방 3개만 열기 ──▶ 사람 20명 ──▶ Q-02 측정
   (DATA-01,03)              (UX-03)         (P1 대화 경험)
```

| 등급 | 뜻 |
|---|---|
| **P0** | 없으면 "동작한다"고 말할 수 없다 |
| **P1** | 핵심 가치(늦게 와도 따라잡는다)가 절반만 구현돼 있다 |
| **P2** | 지금은 되는데 믿고 키울 수는 없다 |
| **P3** | PWA를 표방하는데 아직 PWA가 아니다 |
| **P4** | 사람을 받으려면 법적·운영적으로 필요하다 |

---

## P0 — 지금 동작하지 않는 것

### [DATA-01] 구글 트렌드 어댑터 (첫 실데이터)
- Phase 3 / **최우선** / M
- 왜: `issues` 테이블에 쓰는 주체가 **아무도 없다.** Supabase 모드의 순위는 시드 12건이 전부다.
  8개 소스 중 구글 트렌드만이 공식·무료·안정적이라 여기가 출발점이다
- [ ] Edge Function 으로 KR 실시간 트렌드 파싱
- [ ] `pg_cron` 1분 주기 등록
- [ ] `rank_snapshots` append + `issues.ranks` jsonb 갱신
- [ ] 소스 장애 시 graceful degradation (한 소스 죽어도 서비스 유지)
- [ ] 어댑터 헬스체크 + 실패 알림
- 완료 조건: 24시간 무중단 수집, 누락률 1% 미만
- 대가: 없음

### ~~[DATA-03] 서버측 HotScore~~ — **철회** (2026-08-28)

생애주기(아카이브) 부분은 Edge Function 에 이미 들어갔다.
**HotScore 서버 계산은 필요 없다는 결론**이 났다 —
서버 정렬은 관문일 뿐이고 최신순이면 충분하다.
컬럼도 지웠다 → [delete_plan.md](delete_plan.md) `DEL-06`

### [UX-03] 상위 N개만 방 개설
- Phase 1 / **최우선** / S
- 왜: [plan.md](plan.md) R5. 12개 방을 다 열면 전부 빈방이 된다.
  콜드 스타트에서 기술 문제보다 먼저 닥치는 문제다
- [ ] HotScore 상위 3개만 입장 가능
- [ ] 나머지는 `곧 열립니다 · 알림 받기` 카드
- [ ] 임계치(참여 예약 수 또는 점수) 도달 시 자동 개설
- [ ] 방이 식으면 신규 발화 차단 → 아카이브 이동
- 완료 조건: 열린 방의 평균 발화 수가 개설 후 10분 내 5건 이상
- 대가: 4위 이슈에 관심 있는 사람은 갈 곳이 없다. 알림 예약으로 완화

---

## P1 — 핵심 가치가 절반만 구현된 것

### [UX-06] 새 글 자동 스크롤 + 새 글 배지
- Phase 1 / **높음** / S
- 왜: `_jumpToBottom()` 이 `_send()` 에서만 호출된다
  ([room_page.dart:120](lib/features/room/room_page.dart#L120)).
  **남이 글을 올려도 화면이 안 내려간다.** 채팅방인데 실시간감이 없다
- [ ] 하단에 붙어 있을 때만 자동 스크롤 (읽던 위치를 뺏지 않는다)
- [ ] 위로 올라가 있으면 `새 글 N개 ↓` 플로팅 배지
- [ ] 배지 탭 → 최하단 이동
- 완료 조건: 두 탭으로 같은 방을 열고 한쪽에서 쓰면 다른 쪽이 즉시 따라간다
- 대가: 없음

### [UX-07] 읽음 위치 복원
- Phase 1 / **높음** / M
- 왜: "나중에 온 사람" 이 이 서비스의 중심인데 **어디까지 읽었는지 알 방법이 없다.**
  Catch-up 카드가 흐름을 요약해줘도 재방문 시 다시 처음부터 스크롤해야 한다
- [ ] 방별 마지막 읽은 글 ID를 로컬 저장 (`shared_preferences` 는 이미 의존성에 들어와 있다)
- [ ] 재진입 시 그 위치로 복원 + `여기까지 읽었습니다` 구분선
- [ ] 순위 카드에 안 읽은 글 수 표시
- 완료 조건: 방을 나갔다 들어오면 읽던 자리에서 시작한다
- 대가: 계정이 없으므로 기기 간 동기화는 불가능하다. 익명성의 대가

### [SUPA-01] 이미지 업로드 연결
- Phase 2 / 높음 / M
- 왜: 버킷·RLS·경로 규약(`{uid}/{uuid}.webp`)까지 다 만들어 뒀는데 클라이언트가 없다.
  "이미지와 글로 자유롭게 토론"이 원본 아이디어의 절반이다
- [ ] **선행: [idea.md](idea.md) `Q-06`(이미지를 정말 열 것인가)에 답한다**
- [ ] `image_picker` → 클라이언트 리사이즈 → WebP 변환
- [ ] Storage 업로드 + 진행률 표시
- [ ] 실패 시 재시도 UI (지금은 조용히 자리표시자로 떨어진다)
- [ ] 5MB 초과 파일을 업로드 전에 거절
- 대가: **이 기능이 법적 위험의 대부분을 만든다.** 불법촬영물·저작권.
  Q-06 답 없이 붙이지 말 것

### [PWA-02] 공유 링크
- Phase 1 / 높음 / S
- 왜: 공유 버튼이 토스트만 띄운다. **카톡 공유가 사실상 유일한 유입 채널**인데 미구현이다
- [ ] Web Share API (미지원 브라우저는 클립보드 폴백)
- [ ] 방 딥링크 `/#/issue/:id` 가 실제로 열리는지 검증
- [ ] `share_target` 수신 라우트 `/share` 구현 (매니페스트엔 이미 선언돼 있다)
- 완료 조건: 카톡으로 보낸 링크가 해당 방을 바로 연다
- 대가: 없음

### [INFRA-03] Realtime Presence 로 접속자 수
- Phase 2 / 중간 / S
- 왜: `issues.live_users` 가 항상 0이다. "몇 명이 지금 보고 있다"는
  실시간 서비스의 체감을 만드는 가장 싼 신호인데 비어 있다
- [ ] 방 진입 시 `channel.track()`, `onPresenceSync` 구독
- [ ] presence 페이로드에 식별 정보를 싣지 않는다 (계정이 없으므로)
- **주의**: `presenceState()` 반환 타입이 supabase_flutter 2.x 안에서 바뀐 적이 있다 (Map → List).
  설치 버전(현재 2.17.2)을 확인하고 붙일 것. 이 위험 때문에 초기 구현에서 뺐다
- 완료 조건: 두 탭으로 같은 방을 열면 2가 보인다
- 대가: presence 는 클라이언트 신고값이라 조작 가능. **순위 계산(참여도)에는 쓰지 말 것**

---

## P2 — 믿고 키울 수 없는 것

### [TEST-01] 테스트 계층 확충
- Phase 1 / **높음** / M
- 왜: 앱 4,514줄에 테스트 236줄, **위젯 테스트 0건.**
  지금까지 잡힌 버그는 전부 `flutter analyze` 가 잡은 것이지 테스트가 잡은 게 아니다
- [ ] Repository 계약 테스트 — 목·Supabase 양쪽에 같은 스위트를 돌린다
- [ ] 위젯 스모크 — 4개 화면이 뜨고 탭 전환이 되는지
- [ ] 골든 테스트 — 순위 카드, Catch-up 카드
- [ ] RLS 테스트(pgTAP) — 남의 글 삭제·카운터 조작·남의 추천 조회가 막히는지
- [ ] CI 커버리지 리포트
- 완료 조건: 백엔드 교체가 테스트로 검증된다
- 대가: 목 리포지토리의 `Timer.periodic` 이 위젯 테스트에서 pending timer 로 걸린다.
  테스트용 주입 지점을 먼저 만들어야 한다

### [TEST-02] 목과 실서버의 동작 동등성
- Phase 2 / 중간 / S
- 왜: 목이 실서버를 정확히 흉내내지 않으면 **목에서 한 테스트가 거짓 안심**이 된다.
  지금 목은 `commentCount` 를 난수로 만들고 실서버는 트리거가 센다.
  도배 차단·RLS 거절 같은 실패 경로는 목에 아예 없다
- [ ] 목에도 레이트 리밋·거절 경로를 흉내내는 스위치
- [ ] 두 구현에 같은 계약 테스트 적용 (TEST-01 과 함께)
- 대가: 목 유지 비용이 늘어난다. `INFRA-04` 를 유지하기로 한 대가

### [PERF-01] 페이지네이션
- Phase 2 / 중간 / M
- 왜: `posts` 300건, `comments` 200건이 **하드 캡이다. 넘으면 조용히 잘린다.**
  인기 방은 하루면 넘긴다. 사용자는 대화가 사라진 걸 모른다
- [ ] 방 진입 시 최근 N건만 로드 → 위로 스크롤 시 `이전 대화 더 보기`
- [ ] Realtime 구독은 최신 구간에만
- [ ] 목록 가상화 확인
- 완료 조건: 글 1,000건짜리 방에서 스크롤이 끊기지 않는다
- 대가: Catch-up 카드의 "가장 많이 반응한 글"이 로드된 구간만 보게 된다
  → 서버측 하이라이트 집계가 필요해진다

### [UX-08] 연결 상태 표시
- Phase 2 / 중간 / S
- 왜: Realtime 이 끊기면 **화면이 조용히 멈춘다.** 사용자는 이슈가 잠잠한 건지
  연결이 죽은 건지 구분할 수 없다
- [ ] 연결 끊김 배너 + 자동 재연결
- [ ] 오프라인일 때 작성창 비활성 + 안내
- [ ] 마지막 갱신 시각 표시
- 대가: 없음

### [A11Y-01] 접근성 기본선
- Phase 2 / 중간 / M
- 왜: 텍스트 배율을 1.3으로 강제로 묶어놨다([delete_plan.md](delete_plan.md) DEL-04).
  큰 글씨가 필요한 사람의 설정을 앱이 무시하고 있다
- [ ] 배율 2.0에서 레이아웃이 안 깨지게 수정 (순위 칼럼 30px, `_MetricRow` 52/44px 고정 폭)
- [ ] 클램프 제거
- [ ] 아이콘 버튼 semantics 라벨 (이모지 반응·추천·신고)
- [ ] 색 대비 WCAG AA 검증 — `textTertiary(#60606E)` 가 배경 대비 미달로 의심된다
- 대가: 없음. 지금 상태가 후퇴다

---

## P3 — PWA를 표방하는데 아직 PWA가 아닌 것

### [PWA-01] PNG 아이콘 세트 교체
- Phase 1 / 높음 / XS
- 왜: SVG 매니페스트 아이콘은 브라우저 지원이 고르지 않다. iOS 홈 화면 추가 시 특히
- [ ] `flutter_launcher_icons` 로 192·512 PNG + maskable PNG + `favicon.png` 생성
- 완료 조건: DevTools > Application > Manifest 경고 0개
- 대가: 없음

### [PWA-03] 서비스워커 · 오프라인 셸
- Phase 4 / 높음 / M
- 왜: **아직 한 번도 검증 안 됐다.** `flutter run` 디버그 모드는 매니페스트조차 만들지 않아
  (`manifest not generated in run mode`) 지금까지 PWA 동작을 본 적이 없다
- [ ] `flutter build web --release` 로 실제 설치·오프라인 검증
- [ ] 앱 셸 + 최근 이슈 캐싱
- [ ] 오프라인 진입 시 아카이브 읽기 허용
- [ ] Lighthouse PWA 90점
- 대가: 캐시 무효화 전략을 잘못 짜면 사용자가 옛 버전에 갇힌다

### [PWA-04] 설치 유도 + iOS 안내
- Phase 4 / 중간 / S
- 왜: `beforeinstallprompt` 훅은 `index.html` 에 심어뒀지만 부르는 쪽이 없다.
  **iOS는 16.4+ & 홈 화면 추가 후에만 웹푸시가 된다** — 안내 없이는 아무도 모른다
- [ ] `window.hotissueInstall()` 호출 배너 (방 진입 후 자연스러운 시점)
- [ ] iOS 사용자에게 공유 → 홈 화면 추가 가이드
- 대가: 배너 타이밍이 나쁘면 이탈을 만든다

### [DEL-03] path URL 전환 + 호스팅
- Phase 4 / 중간 / S — 상세는 [delete_plan.md](delete_plan.md)
- 왜: `/#/issue/abc` 는 공유 링크로 보기 나쁘고 SEO에 불리하다
- [ ] 호스팅 SPA rewrite 먼저 (`/**` → `/index.html`)
- [ ] `usePathUrlStrategy()` + 매니페스트 shortcuts URL 수정
- [ ] 기존 해시 URL 리다이렉트
- 대가: rewrite 없이 배포하면 새로고침 시 404. **순서를 지킬 것**

### [PWA-05] OG 이미지 동적 생성
- Phase 4 / 낮음 / M
- 왜: 카톡 공유 미리보기가 유입의 첫인상이다. 지금은 정적 텍스트뿐
- [ ] 이슈별 OG 이미지(키워드 + 순위 + 참여 수) 생성 엔드포인트
- 대가: 렌더링 비용. 캐싱 필요

### [PWA-06] 초기 로딩 최적화
- Phase 4 / 중간 / M
- 왜: Flutter Web 번들은 무겁다. 이슈 서비스는 링크 클릭 후 3초가 전부다
- [ ] 번들 분석 → `--wasm` 빌드와 비교
- [ ] 한글 폰트 서브셋 (Pretendard 도입 시)
- [ ] 스플래시 체감 개선(적용됨) 유지 확인
- 대가: wasm 은 브라우저 지원 확인 필요

---

## P4 — 사람을 받으려면 필요한 것

### [LEGAL-01] 데이터 수집 법률 검토
- **베타 오픈 전 필수**
- 왜: [plan.md](plan.md) R1. 부정경쟁방지법 (파)목, 저작권법, 각 사이트 약관.
  **이 프로젝트에서 기술보다 위험한 영역이다**
- [ ] **최우선 확인: RSS 리드 인용** — 언론사 RSS `<description>` 의 기사 리드를
      방 브리핑에 표시한다 (2026-08-28~). 배포 목적으로 제공된 값이고 본문은
      가져오지 않으며 언론사명·원문 링크를 항상 병기하지만, 인용 범위를 확인받을 것
- [ ] **최우선 확인: 기사 썸네일 핫링크** — 구글 트렌드가 주는 언론사 사진을
      `encrypted-tbn*.gstatic.com` 에서 그대로 불러 카드에 띄우고 있다 (2026-08-28~).
      복제는 하지 않고 URL 만 보관하지만 표시 자체가 쟁점이 될 수 있다.
      문제가 되면 `issues.image_url` 컬럼은 두고 클라이언트 표시만 끄면 된다
- [ ] 소스별 수집 방식 정리 문서
- [ ] 외부 법률 자문
- [ ] 소스별 `허용 / 조건부 / 중단` 판정표
- 대가: 일부 소스를 포기하게 될 수 있다. 그래도 베타 전에 알아야 한다

### [LEGAL-02] 정치 이슈 취급 정책
- **베타 오픈 전 필수**
- 왜: [idea.md](idea.md) Q-05. 한국에서 이슈 서비스의 최대 지뢰밭.
  코드보다 먼저 답이 나와야 한다
- [ ] 선거법 관련 기간 제한
- [ ] 방 개설 제외 기준
- [ ] 운영자 판단 기준표
- 대가: 어떤 정책이든 한쪽에서 비판받는다. 일관성으로 버틴다

### [SAFETY-01] 신고 → 임시조치 파이프라인 (운영자 단계)
- Phase 5 / **최우선** / L
- 왜: 자동 블라인드(5건)는 붙였지만 **사람이 검토하는 단계가 없다.**
  정보통신망법 제44조의2 는 자동화만으로 충족되지 않는다
- [ ] 운영자 검토 큐 + 확정/복원
- [ ] 전 과정 감사 로그
- [ ] 신고자 레이트 리밋 (신고 자체가 공격 수단이 될 수 있다)
- [ ] 자동 블라인드 임계치 5건을 실 트래픽으로 재조정
- 완료 조건: 신고부터 블라인드까지 5분 이내, 운영자 확정까지 24시간 이내

### [OPS-01] 약관 · 개인정보처리방침 · 커뮤니티 가이드
- Phase 5 / 최우선 / M
- 왜: 익명이어도 IP는 개인정보다. 보관 목적·기간을 명시하지 않으면 위법이다
- [ ] 이용약관
- [ ] 개인정보처리방침 (IP 보관 목적·기간·파기)
- [ ] 커뮤니티 가이드라인
- [ ] 앱 내 노출 (설정 탭 `커뮤니티 이용 규칙` 자리는 준비돼 있다)

### [SAFETY-03] 자동 필터
- Phase 5 / 높음 / M
- [ ] 욕설·혐오 사전 필터
- [ ] LLM 보조 분류 (Claude API)
- [ ] 이미지 유해성 검사 (SUPA-01 선행)

---

## 등급 없음 — 하면 좋지만 지금은 아닌 것

### [DATA-02] 키워드 정규화·클러스터링
- Phase 3 / L — 수집 소스가 2개 이상 붙은 뒤에 의미가 생긴다
- 왜: `손흥민` / `손흥민 골` / `토트넘 손흥민` 이 3개 방이 되면 서비스가 망한다
- [ ] 규칙 기반 정규화 → 문자열 유사도 → 임베딩 클러스터링 → 대표 키워드
- [ ] 운영자 수동 병합 도구
- 완료 조건: 수동 검수 100건에서 오분류 5건 이하
- 대가: 과병합하면 무관한 이슈가 한 방에 섞인다. 임계치를 보수적으로

### [DATA-04] LLM 이슈 요약
- Phase 3 / M — Catch-up 카드 품질의 핵심이지만 실데이터가 먼저다
- [ ] 이슈 1줄 요약 (저비용 모델, 5분 주기)
- [ ] 방 흐름 요약 (상위 모델, 30분 주기)
- 완료 조건: 샘플 100건에서 사실 오류 3건 이하
- 대가: API 비용이 방 수에 비례. 상위 N개 방에만 적용

### [CORE-03] 가중치 튜닝 도구
- Phase 1 / S — 실데이터가 없으면 튜닝할 것도 없다
- [ ] 디버그 빌드 전용 슬라이더 패널 (`kDebugMode` 가드 필수)

### [UX-04] 반대 의견 하이라이트
- Phase 3 / M — [idea.md](idea.md) IDEA-09. 에코 챔버 방지
- [ ] 반응 분산도(엔트로피) 지표
- [ ] `의견이 갈리는 글` 슬롯 (중립적 문구로)
- 대가: 갈등을 증폭시킬 위험

### [UX-05] 순위 변동 애니메이션
- Phase 1 / M
- [ ] 순위 이동 애니메이션 + 상승 하이라이트
- [ ] 스크롤 중에는 갱신을 큐에 넣고 `새 순위 보기` 버튼
- 대가: 스크롤 중 순위가 바뀌면 읽던 위치를 잃는다

---

## 폐기된 항목

### ~~[INFRA-01] Firestore 읽기 비용 대안 검토~~ — 무의미해짐 (2026-08-27)

Supabase 채택으로 읽기당 과금 구조 자체가 사라졌다 ([idea.md](idea.md) IDEA-11).
[plan.md](plan.md) 리스크 R6 도 함께 해소됨.
