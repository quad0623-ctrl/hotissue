# 핫이슈(HotIssue) — 실행 계획

> 아이디어를 현실화하기 위한 마스터 플랜.
> 기능 추가는 [add_plan.md](add_plan.md), 제거는 [delete_plan.md](delete_plan.md),
> 아이디어 원본과 변천은 [idea.md](idea.md)에 기록한다.

- 최초 작성: 2026-08-27
- 최종 수정: 2026-08-28
- 현재 단계: **Phase 3 진행 중** — 서버리스 수집 가동, Supabase 배포 준비 완료

---

## 1. 한 문장 정의

> **한국에서 지금 무슨 일이 벌어지는지를 자체 순위로 보여주고, 이슈마다 즉석 토론방을 열어, 늦게 온 사람도 흐름을 따라잡게 하는 PWA.**

## 2. 왜 지금, 왜 이게 필요한가

한국의 실시간 검색어 문화는 사라졌지만 수요는 남았다.

| 서비스 | 상태 |
|---|---|
| 다음 실시간 이슈 검색어 | 2020년 2월 종료 |
| 네이버 실시간 급상승 검색어 | 2021년 2월 종료 |
| 네이트 실시간 이슈 키워드 | 운영 중 |
| 줌(zum) 이슈 검색어 | 운영 중 |
| 구글 트렌드 실시간 인기 | 운영 중 (RSS 제공) |

포털이 손을 뗀 이유는 명확하다. **여론 조작, 총공, 광고 어뷰징, 그리고 "왜 이게 1위냐"는 책임 문제.**
우리가 그 자리에 다시 들어가려면 같은 실수를 반복하면 안 된다.

**우리의 답: 검색량을 베끼지 않는다. 다시 계산하고, 계산식을 공개하고, 사용자가 정렬 기준을 고르게 한다.**

그리고 기존 실검이 못 한 걸 한다 — **순위를 클릭하면 사람들이 이미 모여 있다.**
검색어는 "뭐가 일어났는지"만 알려줬지, "사람들이 뭐라고 하는지"는 다른 데 가서 찾아야 했다.

## 3. 핵심 사용자 루프

```
     ┌─────────────────────────────────────────────┐
     │                                             │
  ①  순위를 본다  ──▶  ②  궁금한 이슈를 누른다        │
     (실시간 탭)          (= 방에 들어감)             │
                              │                     │
                              ▼                     │
                   ③  이미 대화가 있다               │
                      (Catch-up 카드로 흐름 파악)     │
                              │                     │
                              ▼                     │
                   ④  반응한다                       │
                      (이모지 / 추천 / 댓글 / 글)     │
                              │                     │
                              ▼                     │
                   ⑤  내 참여가 순위를 바꾼다  ────────┘
                      (Engagement → HotScore)
                              │
                              ▼
                   ⑥  이슈가 식으면 아카이브로
                      (나중에 온 사람이 읽는 기록)
```

이 루프의 핵심은 **⑤**다. 외부 검색량만 반영하면 우리는 그냥 미러 사이트다.
사용자의 참여가 순위에 반영되어야 "여기서만 볼 수 있는 순위"가 된다.

## 4. 경쟁 대비 차별점

| 대안 | 한계 | 우리 |
|---|---|---|
| 시그널/실검 모음 사이트 | 순위만 보여주고 끝. 체류 0초 | 순위 → 방 → 참여로 이어짐 |
| 커뮤니티(디시/에펨/더쿠) | 이슈를 찾으려면 글목록을 뒤져야 함 | 이슈 단위로 자동 개설 |
| 오픈채팅 | 검색해서 찾아 들어가야 함. 로그가 안 남음 | 순위에서 바로 진입. 기록이 남음 |
| 뉴스 앱 댓글 | 기사 종속. 기사 없으면 대화 없음 | 키워드 종속. 기사보다 빠름 |

**가장 방어하기 쉬운 해자는 "아카이브"다.** 6개월 뒤에도 "그때 사람들이 뭐라고 했지"를
읽을 수 있는 곳은 우리뿐이다. 데이터가 쌓일수록 강해진다.

---

## 5. 자체 정렬 로직 (HotScore)

구현: [lib/domain/ranking/hot_score.dart](lib/domain/ranking/hot_score.dart)
테스트: [test/hot_score_test.dart](test/hot_score_test.dart)

```
HotScore = ( Ws·순위 + Wv·상승세 + Wd·확산도 + We·참여도 ) / ΣW × 신선도 × 100
```

| 항목 | 정의 | 왜 넣었나 |
|---|---|---|
| **순위** (source) | 등장한 소스들의 가중 평균 순위 품질. 1위=1.0, 20위=0.0 | 기본 신호 |
| **상승세** (velocity) | 직전 스냅샷 대비 순위 변화. 신규 진입은 0.9 | 이미 정점 찍은 것보다 지금 뜨는 게 재밌다 |
| **확산도** (diversity) | 동시 등장 소스 수 / (전체 소스 수 ÷ 2), 단일 소스는 0.15 | **어뷰징 방어의 핵심.** 한 곳만 조작하는 건 쉽지만 8곳을 동시에 조작하긴 어렵다 |
| **참여도** (engagement) | `log(1+가중활동량) / log(1+400)` | 우리 서비스만의 신호. 로그 스케일로 눌러 거대 방 독점 방지 |
| **신선도** (freshness) | `0.5 ^ (미관측시간 / 반감기)` | 곱셈 항. 안 쓰면 오래된 이슈가 영원히 남는다 |

**정렬 모드 프리셋** (사용자가 직접 선택)

| 모드 | 순위 | 상승세 | 확산도 | 참여도 | 반감기 | 쓰임 |
|---|---|---|---|---|---|---|
| 균형 | 0.35 | 0.25 | 0.15 | 0.25 | 90분 | 기본값 |
| 속보 | 0.25 | 0.45 | 0.15 | 0.15 | 40분 | 사건 터졌을 때 |
| 토론 | 0.20 | 0.10 | 0.10 | 0.60 | 6시간 | 밤에 눌러앉을 때 |

**알고리즘을 공개하는 이유**: 실검이 죽은 진짜 원인은 불투명성이었다.
카드의 점수를 누르면 5개 세부 점수가 막대그래프로 나온다. "왜 이게 1위냐"에 답할 수 있어야 한다.

### 튜닝이 필요한 지점 (Phase 1)

- `engagementSaturation = 400`은 근거 없는 초기값. 실 트래픽 분포 보고 p90 기준으로 재설정.
- `diversity`의 분모(전체 소스 수 ÷ 2)는 소스가 늘면 의미가 바뀐다. 절대 개수 기반으로 바꿀지 검토.
- 반감기는 시간대별로 달라야 할 수 있다 (새벽 이슈는 천천히 식음).

---

## 6. 데이터 소스 — 현실 점검

**이게 이 프로젝트에서 가장 위험한 부분이다.** 기술이 아니라 법과 정책이 병목이다.

### 6.1 소스별 접근 방법

| 소스 | 방법 | 안정성 | 비고 |
|---|---|---|---|
| 구글 트렌드 (KR) | 공개 RSS / Trends API | 높음 | **1순위.** 공식 채널, 무료 |
| 유튜브 인기 급상승 | YouTube Data API v3 (`chart=mostPopular&regionCode=KR`) | 높음 | 공식 API, 일일 쿼터 존재 |
| 네이버 데이터랩 | 검색어트렌드 오픈API | 높음 | **키워드를 우리가 지정해야 함** → 발굴용이 아니라 *검증용* |
| 네이트 실시간 이슈 | HTML 파싱 | 중간 | 운영 중. 마크업 변경에 취약 |
| 줌 이슈 검색어 | HTML 파싱 | 중간 | 동일 |
| 시그널(signal.bz) | 비공식 엔드포인트 | 낮음 | 3rd party 집계. 의존도 낮게 유지 |
| 포털 뉴스 랭킹 | HTML 파싱 | 중간 | robots.txt 확인 필수 |
| 커뮤니티 인기글 | HTML 파싱 | 낮음 | 약관 리스크 가장 큼 |

### 6.2 지켜야 할 선

- **공식 API 우선.** 크롤링은 대체재가 없을 때만.
- `robots.txt` 준수. `User-Agent` 명시. 요청 간격 최소 60초.
- **키워드/순위만 수집한다.** 기사 본문·이미지는 복제하지 않고 **원문 링크만** 건다.
- 출처 표기 필수 (카드에 소스 뱃지로 이미 노출 중).
- 수집 원본은 `snapshots/` 컬렉션에 90일 보관 후 폐기 (분쟁 대비 + 용량 관리).

관련 법령: 저작권법 제35조의5(공정이용), 부정경쟁방지법 제2조 제1호 (파)목(성과물 무단사용).
데이터 스크래핑 관련 국내 판례가 축적된 영역이므로, **베타 오픈 전 법률 검토를 받는다.**
→ [add_plan.md](add_plan.md) `LEGAL-01`

### 6.3 키워드 정규화 — 조용한 킬러 이슈

`손흥민` / `손흥민 골` / `토트넘 손흥민`이 **서로 다른 3개 방**이 되면 서비스가 망한다.
사람이 분산되어 어느 방도 안 뜨겁다.

해결 순서:
1. 규칙 기반: 공백/특수문자 정규화, 조사 제거, 자모 분리 오타 교정
2. 문자열 유사도(자카드/편집거리)로 1차 묶기
3. 임베딩 기반 클러스터링 → 대표 키워드 선정 (Phase 3)
4. 운영자 수동 병합 도구 (Phase 5)

---

## 7. 아키텍처

```
┌──────────────── 클라이언트 ────────────────┐
│  Flutter Web (PWA)                        │
│  Riverpod · go_router                     │
│  ├ 실시간 탭  ├ 이슈방  ├ 아카이브  ├ 설정   │
│                                           │
│  백엔드 설정이 없으면 목으로 자동 폴백        │
└────────────┬──────────────────────────────┘
             │ Realtime 구독 / PostgREST / Storage
┌────────────▼──────────────────────────────┐
│  Supabase                                 │
│  ├ Auth (익명 전용, 이메일·SMS 비활성)       │
│  ├ Postgres + RLS (이슈·글·댓글·반응·신고)   │
│  ├ Realtime (issues · posts · comments)   │
│  ├ Storage (post-images)                  │
│  ├ 트리거 (카운터 · 도배차단 · 자동블라인드)  │
│  └ RPC (추천/반응 원자적 토글)               │
└────────────▲──────────────────────────────┘
             │ 5분 주기 (pg_cron → pg_net)
┌────────────┴──────────────────────────────┐
│  Edge Function `collect`  ✅ 가동 중        │
│  ├ 구글 트렌드 RSS (이슈 정의)               │
│  ├ 언론사 6곳 19개 피드 (보도 검증 → 확산도)  │
│  ├ 아카이브 전환 (6시간 미관측)              │
│  ├ 키워드 정규화 · 클러스터링    ⬜ DATA-02   │
│  └ LLM 요약 (Claude API)        ⬜ DATA-04   │
└───────────────────────────────────────────┘

로컬 `collector/` 는 같은 로직의 Dart 판이다. 오프라인에서 파서를 고치거나
Edge Function 과 동작을 비교할 때 쓴다. **판단 규칙을 바꾸면 양쪽을 함께 고쳐야 한다.**
```

### 왜 Firebase가 아니라 Supabase인가

초안에는 Firebase로 적었다가 뒤집었다. 판단 근거는 [idea.md](idea.md) IDEA-11 에 남겼다.

**핵심은 RLS다.** 익명 서비스에서 가장 위험한 상태는 "누가 무엇을 할 수 있는가"가
애플리케이션 코드에만 적혀 있는 것이다. Postgres 정책으로 선언해 두면 클라이언트가
뚫려도 DB가 막고, SQL로 테스트할 수 있고, CI에서 검증된다.

부수 효과 세 가지가 따라왔다.

1. **카운터를 트리거로 처리** → 별도 함수 런타임이 필요 없다
2. **읽기당 과금이 사라짐** → 아래 R6 리스크가 통째로 소멸
3. **추천/반응을 RPC로 원자화** → 경쟁 상태와 중복 추천이 코드가 아니라 스키마로 막힌다

**대가**: Realtime 스트림이 조인을 지원하지 않아 `issues.ranks` 를 jsonb 로
비정규화해야 했다. 수집기가 정규 테이블(`rank_snapshots`)과 jsonb 를 둘 다 갱신해야 한다.

### HotScore는 어디서 계산하나

**클라이언트에서만.** 서버는 점수를 계산하지 않는다.

처음엔 "서버가 권위값을 저장하고 클라이언트가 모드별로 재계산" 을 생각했지만,
같은 공식을 두 곳에서 유지하는 비용이 이득보다 컸다.
실제로 서버 점수를 비워둔 채 그걸로 정렬했다가
**활성 68건 중 9건만 화면에 뜨는 사고**가 났다 → [delete_plan.md](delete_plan.md) `DEL-06`

서버가 하는 일은 *어느 행을 보낼지* 고르는 관문뿐이고, 거기엔 `last_seen_at desc` 면
충분하다. 6시간이 지나면 아카이브로 빠지므로 오래된 것이 자리를 차지할 수 없다.

---

## 8. 데이터 모델 (Postgres)

구현: [supabase/migrations/](supabase/migrations/)

```
issues            keyword, normalized_keyword(unique), aliases[], summary, status,
                  ranks jsonb, related_keywords[],
                  posts_count, comments_count, likes_count, reactions_count, live_users,
                  hot_score, first_seen_at, last_seen_at

rank_snapshots    issue_id, source, rank, previous_rank, observed_at   ← 수집 이력. 클라이언트 접근 없음

posts             issue_id, author_id, nickname, color_seed,
                  body, image_path,
                  likes_count, comments_count, reaction_counts jsonb, report_count,
                  is_pinned, is_hidden, created_at

comments          post_id, issue_id, author_id, nickname, color_seed, body, is_hidden

post_likes        (post_id, user_id) PK, issue_id      ← 복합 PK = 1인 1추천이 스키마 보장
post_reactions    (post_id, user_id, emoji) PK, issue_id
reports           target_type, target_id, reporter_id, reason, resolved
                  unique(target_type, target_id, reporter_id)
```

**설계 의도**

- **`issues.ranks` 를 jsonb 로 비정규화**한 게 가장 큰 결정이다.
  Realtime `.stream()` 은 조인을 지원하지 않는다. 정규화된 `issue_ranks` 테이블만
  두면 카드 하나 그리는 데 N+1 구독이 필요하다. jsonb 스냅샷을 issues 행에 얹어
  **한 번의 스트림으로 카드가 완성**되게 했다. 이력은 `rank_snapshots` 에 따로 쌓는다.
- **카운터는 전부 트리거.** 클라이언트에 UPDATE 정책을 주지 않았으므로
  `likes_count` 를 조작할 방법이 없다.
- **복합 PK가 곧 비즈니스 규칙.** `post_likes(post_id, user_id)` 하나로
  중복 추천 방지 코드가 통째로 사라진다.
- **`users` 테이블이 없다.** 계정 제도가 없으니 프로필도 없다.
  레이트 리밋은 `posts` 의 최근 작성 시각을 직접 세는 트리거로 처리한다.
- **`hot_score` 컬럼이 없다.** 서버는 "어느 행을 보낼지" 만 고르면 되고
  그건 `last_seen_at desc` 로 충분하다. 화면 순위는 클라이언트가 정렬 모드별로
  계산한다 → [delete_plan.md](delete_plan.md) `DEL-06`

### RLS 요점

| 대상 | 읽기 | 쓰기 |
|---|---|---|
| `issues` | 전체 공개 | 없음 (수집기는 service_role) |
| `posts` / `comments` | 전체 공개 | 본인 INSERT/DELETE. **UPDATE 정책 없음** |
| `post_likes` / `post_reactions` | **본인 것만** | 본인만 |
| `reports` | 본인 것만 | 본인 INSERT |
| `rank_snapshots` | 없음 | 없음 |

- **글 수정을 막은 이유**: 익명 서비스에서 사후 수정은 "추천 받은 뒤 내용 바꾸기"
  어뷰징 통로다. 지우고 다시 쓰게 한다.
- **추천 행을 본인만 보게 한 이유**: 누가 무엇에 반응했는지가 드러나면 익명이 아니다.
  집계 숫자는 비정규화 컬럼으로 공개하므로 화면에는 지장이 없다.
- 본문 500자, 댓글 300자, 이미지 5MB 는 CHECK 제약과 버킷 설정으로 DB에서 강제한다.
- 스토리지 경로는 `post-images/{uid}/…` 규약이고, 첫 폴더가 본인 uid 여야만 업로드된다.

---

## 9. 단계별 실행 계획

### Phase 0 — 환경 구성 + 목 프로토타입 ✅ 완료 (2026-08-27)

- [x] Flutter 프로젝트 스캐폴드 (`web` 타깃 우선)
- [x] PWA 매니페스트 / 아이콘 / 스플래시 / 설치 프롬프트 훅
- [x] 도메인 모델 (Issue, SourceRank, Post, Comment, RoomStats)
- [x] **HotScore 엔진 + 단위 테스트 8종**
- [x] Riverpod Provider 구조, Repository 인터페이스 분리
- [x] 목 데이터 레이어 (3초 주기로 순위 흔들림)
- [x] 실시간 탭 / 이슈방 / 아카이브 / 설정 4화면
- [x] 점수 근거 공개 시트, 정렬 모드 선택기, Catch-up 카드

### Phase 1 — 프로토타입 완성도 (D+4 ~ D+10)

- [ ] 순위 변동 애니메이션 (`AnimatedList` 또는 `ImplicitlyAnimatedList`)
- [ ] Catch-up 카드 고도화: 시간대별 대화량 스파크라인
- [ ] 이미지 첨부 UI (로컬 미리보기까지, 업로드는 Phase 2)
- [ ] 최근 본 이슈 / 내가 쓴 글 로컬 저장 (`shared_preferences`)
- [ ] 빈 상태·에러 상태·로딩 스켈레톤
- [ ] 가중치 실시간 조절 슬라이더 (디버그 모드 전용) — 튜닝 도구
- [ ] 위젯 테스트 + 골든 테스트

### Phase 2 — 백엔드 연결 (진행 중)

- [x] Supabase 스키마 · 인덱스 · 스토리지 버킷
- [x] **익명 인증** (로그인 화면 없음) + 방별 닉네임 생성 로직
- [x] `SupabaseIssueRepository` 구현 → Provider 한 줄 교체
- [x] RLS 정책 + Realtime 발행
- [x] 카운터 트리거 · 도배 차단 · 신고 자동 블라인드
- [x] 추천/반응 원자적 토글 RPC
- [x] 백엔드 미설정 시 목 폴백 → [add_plan.md](add_plan.md) `INFRA-04`
- [ ] Storage 이미지 업로드 → [add_plan.md](add_plan.md) `SUPA-01`
- [ ] Realtime Presence 접속자 수 → [add_plan.md](add_plan.md) `INFRA-03`
- [ ] RLS 정책 자동 테스트 (pgTAP 또는 통합 테스트)

### Phase 3 — 실제 수집기 (D+25 ~ D+45)

- [ ] 소스 어댑터: 구글 트렌드 → 유튜브 → 네이트/줌 → 뉴스 랭킹 순
- [ ] Cloud Scheduler 1분 주기 파이프라인
- [ ] 키워드 정규화 + 클러스터링
- [ ] 서버측 HotScore 계산 및 저장
- [ ] LLM 이슈 요약 (Claude API — 저비용 모델로 1줄 요약, 방 요약은 상위 모델)
- [ ] 스냅샷 보관/폐기 배치
- [ ] 소스 장애 시 graceful degradation (한 소스 죽어도 서비스 유지)

### Phase 4 — PWA 완성 (D+46 ~ D+60)

- [ ] 서비스 워커 커스터마이즈 (앱 셸 + 최근 이슈 오프라인 캐시)
- [ ] 설치 유도 배너 (`beforeinstallprompt` 훅은 이미 심어둠)
- [ ] 웹푸시 (FCM) — **iOS는 16.4+ & 홈 화면 추가 시에만 가능**, 안내 문구 필요
- [ ] Web Share API + `share_target` 수신 처리 (`/share` 라우트)
- [ ] 해시 URL → path URL 전환 + 호스팅 rewrite
- [ ] 초기 로딩 최적화: 폰트 서브셋, `--wasm` 빌드 비교, 번들 분석
- [ ] Lighthouse PWA 90점 이상
- [ ] OG 이미지 동적 생성 (카톡 공유 미리보기)

### Phase 5 — 운영 가능한 서비스 (D+61 ~)

- [ ] 신고 → 임시조치 파이프라인 (정보통신망법 제44조의2 대응)
- [ ] 자동 필터: 욕설/혐오/도배 + LLM 보조 분류
- [ ] 레이트 리밋 (분당 글 수, 신규 유저 제한)
- [ ] 이미지 유해성 검사
- [ ] 운영자 대시보드 (키워드 병합, 방 잠금, 유저 차단)
- [ ] 이용약관 / 개인정보처리방침 / 커뮤니티 가이드라인
- [ ] 클로즈드 베타 → 오픈

---

## 10. PWA로 만드는 이유와 대가

**이유**
- 앱스토어 심사 없이 배포. 이슈 서비스는 **속도가 생명**이다.
- 링크 하나로 진입. 카톡으로 공유되는 순간이 우리의 유입 채널이다.
- 설치 마찰 0. "이슈 하나 보려고 앱을 깔진 않는다."

**대가와 대응**

| 제약 | 대응 |
|---|---|
| iOS 웹푸시가 홈 화면 추가 후에만 동작 (16.4+) | 방 진입 시 "홈 화면에 추가" 안내를 자연스럽게 노출 |
| Flutter Web 초기 번들이 무겁다 | 스플래시로 체감 완화(적용됨), 폰트 서브셋, wasm 빌드 비교 |
| 검색엔진 노출이 약하다 | 아카이브 페이지만 SSR/프리렌더 별도 구축 검토 (Phase 5) |
| iOS Safari 백그라운드 제약 | 실시간 갱신은 포그라운드 한정으로 설계 |

---

## 11. 리스크 등록부

| # | 리스크 | 영향 | 대응 |
|---|---|---|---|
| R1 | 데이터 수집의 법적 분쟁 | **치명** | 공식 API 우선, 키워드만 수집, 원문 링크, 베타 전 법률 검토 |
| R2 | 익명 채팅의 명예훼손·혐오 | **치명** | 신고·임시조치, 자동 필터, 로그 보관, 운영자 개입 |
| R3 | 소스 마크업 변경으로 수집 중단 | 높음 | 소스 다변화, 어댑터별 헬스체크, 알림 |
| R4 | 키워드 분산으로 방이 안 뜨거움 | 높음 | 클러스터링(§6.3), 방 병합 도구 |
| R5 | 초기 사용자 없어 방이 텅 빔 | 높음 | 아카이브 우선 노출, 시드 콘텐츠, 소수 이슈에 집중 |
| R6 | ~~Firestore 읽기 비용 폭증~~ | — | **해소됨.** Supabase 채택으로 읽기당 과금 구조가 사라짐 (IDEA-11) |
| R7 | 여론 조작 세력 유입 | 중간 | 확산도 가중치, 레이트 리밋, 신규 계정 제한 |
| R8 | Flutter Web 성능 불만 | 중간 | 목록 가상화, 이미지 지연 로딩, wasm |

**R5가 실질적으로 가장 먼저 닥친다.** 순위 12개를 다 열어두면 전부 빈방이 된다.
초기에는 **상위 3개 이슈만 방을 열고** 나머지는 "곧 열립니다"로 두는 게 낫다.
→ [add_plan.md](add_plan.md) `UX-03`

---

## 12. 성공 지표

| 단계 | 지표 | 목표 |
|---|---|---|
| 프로토타입 | 순위 → 방 진입률 | 30% 이상 |
| 프로토타입 | 방 진입 후 1분 이상 체류 | 50% 이상 |
| 베타 | Catch-up 카드 열람률 | 70% 이상 (핵심 가설 검증) |
| 베타 | 방당 하루 발화 수 | 상위 3개 방 각 100건 |
| 베타 | D1 리텐션 | 20% 이상 |
| 베타 | PWA 설치 전환율 | 방문자의 5% |

**가장 중요한 건 Catch-up 카드 열람률이다.** 이게 낮으면
"늦게 와도 따라잡을 수 있다"는 이 서비스의 전제가 틀린 것이므로, 방향을 바꿔야 한다.

---

## 13. 지금 당장 할 일

```powershell
cd c:\Users\USER\ai_service\hotissue

# 1) 목 데이터로 화면 확인 (Supabase 없어도 됨)
.\run_local.ps1

# 2) 랭킹 엔진 테스트
.\run_local.ps1 -Test

# 3) Supabase 붙이기
supabase start ; supabase db reset
Copy-Item env\dev.example.json env\dev.json    # 출력된 URL/anon key 채우기
.\run_local.ps1 -Supabase

# 4) Git 초기화
git init ; git add . ; git commit -m "핫이슈 Phase 0-1"

# 5) (선택) 안드로이드/iOS 네이티브 폴더 — web/ 은 건드리지 않음
flutter create . --platforms=android,ios --org kr.hotissue
```

> 스캐폴드와 Supabase 연동 코드는 셸을 쓸 수 없는 환경에서 수동 작성되었다.
> **`flutter analyze` 로 한 번 검증한 뒤 커밋할 것.**
> `dart format .` 도 아직 안 돌렸다 → [add_plan.md](add_plan.md) `CI-01`
>
> `web/` 아이콘은 SVG다. 배포 전 192/512 PNG로 교체 → `PWA-01`
