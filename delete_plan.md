# 제거 계획 로그

> **빼기로 한 것**의 기록. 코드든 기능이든, 지우기 전에 여기 먼저 쓴다.
> 추가는 [add_plan.md](add_plan.md), 전체 로드맵은 [plan.md](plan.md).

**왜 제거를 따로 기록하는가**

프로토타입은 임시 코드로 굴러간다. 그 임시 코드가 언제 왜 사라져야 하는지를
적어두지 않으면 6개월 뒤에 아무도 못 지운다. "이거 왜 있지? 무서우니까 두자"가
쌓이면 프로젝트가 굳는다.

**기록 규칙**
- 지우기 **전에** 항목을 만든다. 지운 다음에 쓰는 건 로그가 아니라 변명이다
- 반드시 **선행 조건**을 적는다. 무엇이 준비되어야 안전하게 지울 수 있는가
- 되돌릴 방법(커밋 해시, 백업 위치)을 남긴다
- 완료하면 `§ 완료`로 이동하고 실제 결과를 적는다

**ID 접두사**

| 접두사 | 의미 |
|---|---|
| `DEL` | 코드/모듈 제거 |
| `FEAT-OFF` | 기능 자체를 접음 |
| `DEP` | 의존성 제거 |
| `DATA-DEL` | 데이터 폐기 |

**항목 형식**

```
### [ID] 제목
- 대상: 지울 파일/기능
- 시점: 언제
- 왜: 왜 지워야 하는가
- 선행 조건: 무엇이 끝나야 안전한가
- 영향: 무엇이 깨질 수 있는가
- 되돌리기: 어떻게 복구하는가
```

---

## 완료

_(아직 없음 — Phase 0에서 만들기만 했다)_

---

## 예정

### [DEL-01] ~~목 데이터 레이어 전체 제거~~ → **판단 뒤집음** (2026-08-27)

원래 "Phase 2에 목을 통째로 지운다"고 적었다. **철회한다.**

Supabase를 붙이면서 알게 된 것: 목이 없으면 Docker가 꺼져 있거나, 네트워크가 없거나,
Supabase 프로젝트를 아직 안 만든 상태에서 **화면을 아예 못 본다.**
프로토타입 단계에서 이건 개발 속도를 직접 깎아먹는다.

목은 "아직 못 지운 임시 코드"가 아니라 **의도된 오프라인 개발 모드**로 승격한다.
→ [add_plan.md](add_plan.md) `INFRA-04`

대신 남는 과제는 아래 DEL-01a 다.

### [DEL-01a] 릴리스 번들에서 목 레이어 제외
- **대상**
  - [lib/data/mock/mock_feed.dart](lib/data/mock/mock_feed.dart)
  - [lib/data/repositories/mock_issue_repository.dart](lib/data/repositories/mock_issue_repository.dart)
- **시점**: Phase 4, 프로덕션 배포 직전
- **왜**: 가짜 키워드 15개와 가짜 대화 12줄이 프로덕션 번들에 실려 나갈 이유가 없다.
  용량 문제라기보다, 실서버 장애 시 사용자에게 **가짜 이슈가 진짜처럼 보이는 사고**가 위험하다
- **선행 조건**
  1. 실서버 실패 시 폴백이 아니라 명시적 오류 화면을 띄우도록 변경
  2. `kReleaseMode` 에서는 폴백 경로를 타지 않게 분기
  3. 목 전용 코드를 조건부 import 또는 별도 flavor 로 분리
- **영향**: 릴리스 빌드에서 네트워크가 끊기면 빈 화면 + 오류 메시지가 된다. 그게 맞다
- **되돌리기**: 분기 한 줄

### [DEL-02] ~~`_ImagePlaceholder` 위젯 제거~~ → **부분 해소** (2026-08-27)
- **현재 상태**: `_PostImage` 가 추가되어 `http`로 시작하는 URL은 `Image.network` 로 뜨고,
  자리표시자는 **로딩·실패·목 URL 폴백** 역할로 남았다. 제거 대상이 아니라 상태 UI가 됐다
- **남은 작업**: 실패 시 재시도 버튼이 없다. 지금은 조용히 자리표시자로 떨어진다
  → [add_plan.md](add_plan.md) `SUPA-01` 과 함께 처리
- **정리해야 할 것**: `MockFeed.generatePosts` 가 만드는 `mock://image/...` URL은
  DEL-01a 때 목 레이어와 함께 빠진다

### [DEL-03] 해시 URL 전략 제거
- **대상**: [lib/core/router/app_router.dart](lib/core/router/app_router.dart) 기본 해시 전략 → `usePathUrlStrategy()`
- **시점**: Phase 4, 호스팅 확정 후
- **왜**: `/#/issue/abc` 는 공유 링크로 보기 나쁘고 SEO에 불리하다.
  아카이브를 검색 유입 채널로 쓰려면 path URL이 필요
- **선행 조건**
  1. 호스팅에 SPA rewrite 규칙 적용 (`/**` → `/index.html`)
  2. `web/manifest.json` 의 `shortcuts` URL을 `#/archive` → `/archive` 로 수정
  3. 이미 배포된 해시 URL에 대한 리다이렉트 처리
- **영향**: rewrite 없이 배포하면 새로고침 시 404. **호스팅 설정이 먼저다**
- **되돌리기**: `usePathUrlStrategy()` 호출 한 줄 제거

### [DEL-04] `MediaQuery.withClampedTextScaling` 하드코딩 제거
- **대상**: [lib/app.dart](lib/app.dart) 의 텍스트 배율 상한 1.3
- **시점**: Phase 4, 접근성 점검 시
- **왜**: 배율을 강제로 묶는 건 접근성 후퇴다. 지금은 레이아웃이 깨지는 걸 막으려는 임시방편
- **선행 조건**: 배율 2.0에서도 모든 화면이 깨지지 않도록 레이아웃 수정
  (특히 `IssueCard` 의 고정 폭 30px 순위 칼럼, `_MetricRow` 의 고정 폭 52/44px)
- **영향**: 큰 글씨 설정 사용자에게 레이아웃 깨짐이 노출될 수 있음
- **되돌리기**: 한 줄 복원

### [DEL-05] 데스크톱 폭 제한(520px) 재검토
- **대상**: [lib/app.dart](lib/app.dart) 의 `maxWidth: 520`
- **시점**: Phase 4
- **왜**: 모바일 우선이라 넣었지만, PC 브라우저 사용자에게는 화면 낭비다.
  아카이브 읽기는 넓은 화면이 유리
- **선행 조건**: 반응형 2단 레이아웃(왼쪽 순위 / 오른쪽 방) 구현
- **영향**: 없음. 제거가 아니라 조건부화
- **되돌리기**: 불필요

---

## 기능 자체를 접기로 한 것

### [FEAT-OFF-01] 실시간 음성/영상 토론
- **시점**: 시작 전 기각 (2026-08-27)
- **왜**: "나중에 온 사람이 읽는다"는 핵심 가치와 정면 충돌.
  음성은 훑어보기가 불가능하고 모더레이션 비용이 폭증한다
- 상세: [idea.md](idea.md) IDEA-X2

### [FEAT-OFF-02] 실명/SNS 로그인 필수
- **시점**: 시작 전 기각 (2026-08-27)
- **왜**: 진입 마찰이 PWA의 유일한 장점을 없앤다
- **재검토 조건**: 레이트 리밋과 신고 시스템으로도 남용이 통제되지 않을 때
- 상세: [idea.md](idea.md) IDEA-X1

---

## 데이터 폐기 정책

### [DATA-DEL-01] 수집 스냅샷 90일 폐기
- **대상**: `snapshots/{yyyyMMddHHmm}`
- **시점**: Phase 3부터 상시 (일 1회 배치)
- **왜**: 저장 비용 + 원본 장기 보관은 저작권 분쟁 시 불리하게 작용할 수 있다.
  집계 결과(`issues/`)만 남기면 충분
- **선행 조건**: 랭킹 재현이 필요한 기간을 확정 (90일이면 충분한지 검증)
- **영향**: 90일 이전 순위 변동의 원본 검증 불가
- **되돌리기**: 불가. **삭제 전 집계 지표를 별도 컬렉션에 적재할 것**

### [DATA-DEL-02] 접속 IP 로그 최소 보관
- **대상**: 익명 사용자 IP 로그
- **시점**: Phase 5, 개인정보처리방침 확정 시
- **왜**: 익명 서비스라도 IP는 개인정보다. 목적 달성에 필요한 최소 기간만 보관
- **선행 조건**
  1. 보관 목적 명시 (남용 차단·수사 협조)
  2. 개인정보처리방침에 보관 기간 명시
  3. 자동 파기 배치 구현
- **영향**: 장기 어뷰저 추적이 어려워짐. 해시 기반 식별자로 일부 보완
- **되돌리기**: 불가
